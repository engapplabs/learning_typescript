function addNums(num1: number, num2: number): number {
	return num1 + num2;
}

function add(element1: any, element2: number) {
	if(typeof element1 == string)
		return element1${element2};
	return element1 + element2;
}

console.log(addNums("10", 505));
